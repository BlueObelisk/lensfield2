<!DOCTYPE lensfield-build [
        <!ELEMENT lensfield-build (version,dependencies?,build-steps?)>
        <!ELEMENT dependencies (dependency*)>
        <!ELEMENT dependency (groupId,artifactId,version)>
        <!ELEMENT groupId (#PCDATA)>
        <!ELEMENT artifactId (#PCDATA)>
        <!ELEMENT version (#PCDATA)>
        <!ELEMENT build-steps (step*)>
        <!ELEMENT step (name,process,inputs?,outputs?,parameters?)>
        <!ELEMENT name (#PCDATA)>
        <!ELEMENT process (#PCDATA)>
        <!ELEMENT inputs (input*)>
        <!ELEMENT input (#PCDATA)>
        <!ELEMENT outputs (output)*>
        <!ELEMENT output (#PCDATA)>
        <!ELEMENT parameters (parameter*)>
        <!ELEMENT parameter (name?,value)>
        <!ELEMENT value (#PCDATA)>
        ]>
<lensfield-build>

    <version>1.0.0</version>

    <dependencies>
        <dependency>
            <groupId></groupId>
            <artifactId></artifactId>
            <version></version>
        </dependency>
        <dependency>
            <groupId></groupId>
            <artifactId></artifactId>
            <version></version>
        </dependency>
    </dependencies>

    <build-steps>

        <step>
            <name></name>
            <process></process>
            <inputs>
                <input></input>
            </inputs>
            <outputs>
                <output></output>
            </outputs>
            <parameters>
                <parameter>
                    <name></name>
                    <value></value>
                </parameter>
            </parameters>
        </step>

    </build-steps>

</lensfield-build>
